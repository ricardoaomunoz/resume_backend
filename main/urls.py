from django.urls import path
from . import views

app_name = "main"

urlpatterns = [
    # path('', views.IndexView.as_view(), name="home"),
    # path('', views.as_view(), name=""),
    # path('', views.as_view(), name=""),
    # path('', views.as_view(), name=""),
    # path('', views.as_view(), name=""),
    # path('', views.as_view(), name=""),    
]